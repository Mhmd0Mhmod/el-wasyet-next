# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files - keep .env.example but ignore actual secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Allow .env.example to be committed (it's documentation)
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# cloudflare deployment files
.open-next
.open-next/*
.wrangler
# Allow wrangler.toml to be committed (it's configuration, not secrets)
!wrangler.toml
# But ignore the wrangler state directory
.wrangler/*

# local webstorm config
/.idea/